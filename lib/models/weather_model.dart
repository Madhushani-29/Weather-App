class WeatherModel {
  final String currentSky;
  final double currentPressure;
  final double currentWindSpeed;
  final double currentHumidity;

  WeatherModel(this.currentSky, this.currentPressure, this.currentWindSpeed, this.currentHumidity);
}
